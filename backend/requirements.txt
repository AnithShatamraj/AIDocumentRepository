# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sse-starlette==2.1.3
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
pgvector==0.3.6
alembic==1.14.0

# --- Background jobs ---
celery[redis]==5.4.0
redis==5.2.1

# --- Auth ---
pyjwt==2.10.1
bcrypt==4.2.1

# --- Object storage (cloud-agnostic) ---
boto3==1.35.90
azure-storage-blob==12.24.0

# --- AI providers ---
# openai >= 2.45 required by langchain-openai 1.3.x; our direct SDK usage is 2.x-compatible.
openai==2.46.0
tiktoken==0.8.0
tenacity==9.0.0

# --- Agentic ingestion (deepagents / LangChain / LangGraph) ---
# Imported lazily; app degrades gracefully to the direct path if unavailable.
# Pinned to the coherent stable 1.x line (deepagents 0.0.6 + the langchain 1.0
# alphas are mutually broken — 0.0.6 imports langchain.agents.tool_node which the
# alpha lacks). These versions resolve cleanly and deepagents imports/runs.
deepagents==0.6.12
langchain==1.3.14
langchain-openai==1.3.5
langgraph==1.2.9

# --- Document parsing ---
pypdf==5.1.0
pdfplumber==0.11.4
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2

# --- Observability ---
structlog==24.4.0

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
